pub mod Interrupt {
    use crate::{enums::interrupt_types::InterruptType, modules::cpu::CPU};

    pub fn request(interrupt_type: InterruptType) {
        //
    }

    pub fn handle(cpu: &CPU) {
        //
    }
}
